'use client';

import React from 'react';

const CatchPhrase = () => (
  <div
    style={{
      position: 'absolute',
      top: '30%',
      left: '50%',
      transform: 'translate(-50%, -30%)',
      textAlign: 'center',
    }}
  >
    <h1 style={{ fontSize: '3rem', color: '#0D47A1', marginBottom: '10px' }}>
      Find the Right Professional for Your Needs
    </h1>
    <p style={{ fontSize: '1.2rem', color: '#555', marginBottom: '20px' }}>
      Discover top-tier experts to help you get the job done efficiently.
    </p>
  </div>
);

export default CatchPhrase;
